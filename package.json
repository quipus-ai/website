{
  "name": "quipus-website",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for quipus.ai, a one-person Linux apps studio.",
  "packageManager": "bun@1.2.19",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
